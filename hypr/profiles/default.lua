return {
	-- Override or extend the base config for the 'default' profile
	variables = {
		-- No overrides, default profile uses base defaults
	},
	autostart = {
		-- Default uses waybar and awww-daemon which are already in base
	},
}
